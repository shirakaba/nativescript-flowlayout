/**
 * @satisfies {import("prettier").Config}
 * @see https://prettier.io/docs/en/configuration.html
 */
const config = {
  // Take all defaults, but leave this here to prevent Prettier climbing up to a
  // user-level config (may just be my superstition).
};

export default config;
