This repo contains the following configs. They are all standalone, but some may supersede (be used instead of) others.

- `imports`
- `javascript`
  - `javascript-typescript` (supersedes `javascript`; use in TypeScript projects)
- `unicorn`
